public class draw {
  public static void main(String[] args){

    
    //Square of specified size, centered at (x,y)
    stdDraw.square(0.2,0.8,0.1);
    stdDraw.filledSquare(
    stdDraw.circle(
    double[] xd = {}
    double[] yd
    stdDraw.filled polygon(xd,yd);
    stdDraw.setPenColor(StdDraw.WHITE);
    StdDraw.text(0.8,-.8,"white text");

    
    StdOut.println("Number of Circles?");
    int num = StdInt.readInt;
    // double r = 0.5;
    // double diff = r/num;
    for((double)i = 1.0; i <= num; i++){
      if(i % 2 = 0){
        StdDraw.setPenColor(StdDraw.BLUE);
      }
      else{
        StdDraw.setPenColor(StdDraw.BLACK);
      }
      stdDraw.filledCircle(1.0,1.0,i)
        //System 1.0,1.0,r
        //r -= diff;
    }



    
  }
}